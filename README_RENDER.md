## Chatwoot Render Deployment

### Build Command:
bash scripts/render-build.sh

### Start Command:
bash bin/render-start.sh

Add Postgres + Redis in Render settings and use .env.example as reference.

